class PropertiesService {
  constructor(service) {
    this.service = service;
  }

  getProperty() {
    return new Promise(function (resolve, reject) {
      resolve();
      reject();
    });
  }

  getProperties(limit, offset, filters) {
    return new Promise(function (resolve, reject) {
      resolve();
      reject();
    });
  }

  getPropertiesByUser(limit, offset, filters) {
    return new Promise(function (resolve, reject) {
      resolve();
      reject();
    });
  }

  createProperties() {
    return new Promise(function (resolve, reject) {
      resolve();
      reject();
    });
  }

  updateProperties() {
    return new Promise(function (resolve, reject) {
      resolve();
      reject();
    });
  }

  deleteProperties() {
    return new Promise(function (resolve, reject) {
      resolve();
      reject();
    });
  }
}
